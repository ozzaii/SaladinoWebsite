import { LoaderContext } from 'webpack';

type LoaderOptions = {
    dir: string;
    isServer: boolean;
    isDev: boolean;
};
declare function loader(this: LoaderContext<LoaderOptions>, content: string): Promise<void>;

export { loader as default };
